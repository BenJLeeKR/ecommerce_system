# PROJECT_STATE.md

## 1. 프로젝트 개요 및 환경
* **목표:** 단일 소비자 직접 판매(Direct-to-Consumer) E-commerce MVP 구축
* **아키텍처 형태:** 모듈형 모놀리스 (Modular Monolith)
* **프론트엔드:** Next.js
* **백엔드:** NestJS, TypeScript, PostgreSQL, Prisma
* **기준 시간대:** KST (Asia/Seoul, UTC+09:00). DB, API, 로그 등의 타임스탬프는 UTC로 저장 및 전송되나 표시 및 비즈니스 규칙 평가는 KST를 기준으로 합니다.

## 2. 확정된 아키텍처 및 거버넌스
* **데이터베이스 접근:** 퍼블릭 PostgreSQL 인스턴스 내의 프로젝트 전용 DB로 제한되며, 전용 최소 권한 계정을 사용합니다.
* **테이블 명명 규칙:** 프로젝트 전용 DB 내에서는 접두사 없이 복수형 snake_case 규칙을 따릅니다.
* **문서 구조 원칙:** 모든 로컬 기술 문서는 루트의 `docs/` 내 구조를 엄격히 준수합니다. `.orchestrator/` 디렉토리는 Git에 추적되는 실행 코드, 테스트 코드, `AGENTS.md`, `.env.example` 파일만 제한적으로 Git에 추적되며 문서 폴더로 사용하지 않습니다. 로컬 `.env` 파일이나 SQLite DB, 기타 문서 파일은 이 경로에 생성 및 추적되지 않습니다.
* **인프라 기준:**
  * 매일 03:00 KST에 자동 백업 수행
  * 백업 보존 주기: 일간 31회, 월간 12회
  * 목표 복구 지점(RPO): 24시간
  * 목표 복구 시간(RTO): 4시간

## 3. 주요 도메인/DB 정책 상태
* **민감 도메인:** 주문(Order), 결제(Payment), 재고(Inventory)는 민감 영역으로 분류됩니다.
* **재고 및 동시성 제어:** 재고 TTL은 15분으로 설정되며, 동시성 제어를 위해 조건부 원자적 업데이트(conditional atomic updates)를 사용합니다.
* **결제 정책:** 결제 재시도 횟수는 최대 3회로 제한됩니다. 복합 결제 멱등성 키(composite payment idempotency keys)를 사용하며 순서가 맞지 않는 콜백에 대한 수동 대사가 적용됩니다.
* **데이터 일관성:** 트랜잭션 및 외래키는 기존 설계 규칙에 의존하며 보상 트랜잭션(Saga 패턴 등)은 MVP가 아닌 모듈 분리 단계로 이관됩니다.
* **프론트엔드 검증 역할:** 재시도 횟수에 따른 버튼 숨김 등 프론트엔드 검증은 1차 방어막 역할을 하며, 백엔드 서버에서 동일한 기준을 다시 한 번 독립적으로 검증하고 차단합니다.

## 4. 미결 사항 및 MVP 제외 항목
* **Codex 권장안 / 사용자 승인 대기:**
  * UUID, Enum, Prisma 매핑 등 세부적인 데이터베이스 스키마와 데이터 타입 매핑 정책은 아직 최종 확정되지 않았으며 승인 대기 상태입니다.
* **MVP 제외 항목:**
  * 리뷰(Review) 도메인은 MVP 범위에서 완전히 제외됩니다. 관련된 어떠한 로직이나 데이터베이스 스키마도 구현되지 않습니다.
  * 보상 트랜잭션 (예: Saga 패턴 등) 및 복잡한 분산 락, 새로운 분산 시스템(예: Kafka, Redis) 도입 또한 MVP에서는 제외되었습니다.

---
**참고:** 본 문서는 `CLAUDE.md`, `AGENTS.md` 및 `docs/` 디렉토리 하위의 승인된 아키텍처 문서들을 기반으로 작성되었습니다.
